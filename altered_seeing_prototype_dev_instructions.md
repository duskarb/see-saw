# Altered Seeing / <see-saw> 프로토타입 개발 지시사항

## 0. 프로젝트 목적

이 문서는 KAIST 산업디자인학과 학과동 1층 상설 전시 공간에서 `Altered Seeing` / `<see-saw>`를 빠르게 실험하기 위한 프로그램 개발 지시사항이다.

이 프로토타입의 목적은 완성된 전시 작품을 만드는 것이 아니라, 다음 구조가 실제 관객 동선에서 작동하는지 검증하는 것이다.

> 관객은 작품 설명을 읽는다고 생각하지만, 실제로는 자신의 읽기 행위로 마지막 설치를 생성한다.

기본 설치 구조는 다음과 같다.

```text
전시장 입구 / 작품 앞 QR 코드
        ↓
모바일 웹 접속
        ↓
가짜 큐레이션 텍스트 읽기
        ↓
읽기 시간, 스크롤, 이탈 지점 수집
        ↓
WebSocket 서버로 실시간 전송
        ↓
전시 끝 / 모니터에서 집단적 읽기 흔적 시각화
```

---

## 1. 작품 구조

### 1.1 Altered Seeing

`Altered Seeing`은 관객이 접속하는 모바일 웹 인터페이스이다.

겉으로는 산업디자인학과 상설 전시의 공식 큐레이션 웹처럼 보인다.  
관객은 QR 코드를 통해 접속하고, 각 전시물에 대한 설명을 읽는다고 생각한다.

하지만 이 인터페이스의 실제 기능은 설명이 아니라, 관객의 읽기 행위 자체를 감지하고 기록하는 것이다.

### 1.2 <see-saw>

`<see-saw>`는 전시장 마지막 혹은 가장 안쪽에 설치되는 모니터 기반 작품이다.

모바일 웹에서 수집된 관객들의 읽기 데이터가 실시간으로 축적되고, 모니터 화면에서 하나의 집단적 텍스트 풍경으로 시각화된다.

이때 중요한 점은 `<see-saw>`가 “데이터 대시보드”처럼 보이면 안 된다는 것이다.  
관객 통계, 숫자, 그래프를 보여주는 것이 아니라, 관객들의 읽기 흔적이 하나의 살아 있는 텍스트 덩어리처럼 변형되어야 한다.

---

## 2. 실험 장소와 물리 설치

### 2.1 장소

- KAIST 산업디자인학과 학과동 1층 상설 전시
- 기존 전시물을 그대로 활용한다.
- 작품 앞 혹은 전시장 초입에 QR 코드가 인쇄된 종이를 부착한다.
- 전시의 마지막 지점이나 가장 안쪽에 모니터 1대를 설치한다.

### 2.2 필요한 물리 장비

- QR 코드 인쇄물 여러 장
- 노트북 1대 또는 미니 PC 1대
- 모니터 1대
- Wi-Fi 환경
- 멀티탭
- 모니터 고정용 스탠드 혹은 책상
- 가능하다면 안내 문구 없이 QR 코드만 부착

### 2.3 권장 설치 방식

QR 종이는 너무 작품처럼 보이면 안 된다.  
학교 전시 안내물처럼 보여야 한다.

권장 문구:

```text
작품 설명 보기
Scan to read exhibition text
```

또는 더 공식적으로:

```text
Industrial Design Permanent Exhibition
Mobile Guide
```

---

## 3. 핵심 경험 시나리오

### 3.1 관객 입장

관객은 상설 전시를 지나가다가 QR 코드를 발견한다.  
QR 코드는 공식 전시 설명 웹처럼 보인다.

### 3.2 모바일 웹 접속

관객은 자신의 스마트폰으로 QR을 찍고 모바일 웹에 접속한다.

첫 화면은 다음 요소를 포함한다.

- 전시명
- 작품명
- 작가명 혹은 학과명
- 연도
- 재료
- 짧은 설명
- 긴 큐레이션 텍스트

### 3.3 읽기 행위

관객이 텍스트를 읽는 동안 웹은 다음 데이터를 수집한다.

- 각 문단이 화면에 보인 시간
- 스크롤 깊이
- 스크롤 속도
- 특정 문단으로 다시 올라갔는지 여부
- 페이지를 닫거나 이탈한 지점
- 접속 시각
- 접속한 QR 코드 위치 또는 작품 ID

### 3.4 마지막 모니터

관객이 전시 마지막 지점에 도착하면 모니터에 텍스트 기반 시각화가 보인다.

가장 오래 읽힌 문장은 점점 커지고 진해진다.  
거의 읽히지 않은 문장은 흐려지거나 사라진다.  
많은 관객이 멈춘 문장은 화면 중심으로 이동한다.  
이탈이 많이 발생한 문장 이후는 끊긴 듯 보인다.

화면은 다음 문장을 직접적으로 말하지 않지만, 구조적으로 암시해야 한다.

> 이 전시에서 가장 많이 읽힌 거짓말이 가장 크게 남는다.

---

## 4. 수집할 데이터

### 4.1 필수 데이터

프로토타입에서는 다음 데이터만 우선 구현한다.

```ts
type ReadingEvent = {
  sessionId: string;
  pageId: string;
  blockId: string;
  eventType: "enter" | "exit" | "heartbeat" | "scroll" | "leave";
  timestamp: number;
  visibleRatio?: number;
  scrollY?: number;
  scrollDepth?: number;
};
```

### 4.2 세션 데이터

```ts
type SessionData = {
  sessionId: string;
  pageId: string;
  startedAt: number;
  endedAt?: number;
  userAgent?: string;
  language?: string;
  screenWidth?: number;
  screenHeight?: number;
};
```

### 4.3 집계 데이터

서버는 클라이언트에서 받은 데이터를 다음 구조로 누적한다.

```ts
type BlockAggregate = {
  blockId: string;
  text: string;
  totalViewTime: number;
  viewCount: number;
  revisitCount: number;
  averageVisibleRatio: number;
};
```

### 4.4 수집하지 말아야 할 데이터

이번 프로토타입에서는 다음 데이터는 수집하지 않는다.

- 이름
- 학번
- 이메일
- 정확한 위치 정보
- 카메라
- 마이크
- 연락처
- 로그인 정보
- 개인 식별 가능한 정보

이 작업의 목적은 개인 추적이 아니라, 관객의 읽기 행위가 집단적으로 어떻게 작품화되는지 확인하는 것이다.

---

## 5. 모바일 웹 상세 지시사항

### 5.1 기본 방향

모바일 웹은 “실험용 웹앱”처럼 보이면 안 된다.  
최대한 진짜 학교 전시 안내 페이지처럼 보여야 한다.

시각 스타일:

- 흰 배경
- 검정 텍스트
- 얇은 구분선
- 작은 로고 혹은 학과명
- 과도한 그래픽 금지
- 기본 산세리프
- 약간 건조하고 공식적인 문체

### 5.2 모바일 웹 페이지 구조

```text
[상단]
Industrial Design Permanent Exhibition

[작품 정보]
Object No. 03
Title
Student / Department
Year
Material

[본문]
Curatorial Text
문단 1
문단 2
문단 3
문단 4
문단 5

[하단]
Department of Industrial Design
KAIST
```

### 5.3 텍스트 블록 처리

각 문단은 반드시 고유한 `data-block-id`를 가져야 한다.

예시:

```html
<p data-block-id="p01">...</p>
<p data-block-id="p02">...</p>
<p data-block-id="p03">...</p>
```

모바일 클라이언트는 `IntersectionObserver`를 사용해 각 문단이 화면에 들어오고 나가는 시간을 측정한다.

### 5.4 권장 큐레이션 텍스트 전략

초기 프로토타입에서는 “끝까지 그럴듯한 거짓 큐레이션 텍스트”를 사용한다.

이유:

- 관객이 쉽게 장난이라고 알아차리면 실험이 무너진다.
- 중요한 것은 텍스트의 이상함이 아니라, 텍스트를 신뢰하며 읽는 행위이다.
- 마지막 설치에서 그 신뢰의 흔적이 드러나야 한다.

---

## 6. 모니터 시각화 상세 지시사항

### 6.1 기본 방향

모니터 화면은 데이터 대시보드가 아니라 작품처럼 보여야 한다.

피해야 할 표현:

- 막대그래프
- 원그래프
- 숫자 통계
- “총 방문자 수”
- “평균 체류 시간”
- 관리자 페이지처럼 보이는 UI

권장 표현:

- 텍스트의 크기 변화
- 문장의 불투명도 변화
- 문장 간 거리 변화
- 오래 읽힌 문장의 화면 중심 이동
- 읽히지 않은 문장의 소멸
- 집단적 읽기 흔적이 쌓이는 느낌

### 6.2 시각화 규칙

각 텍스트 블록은 다음 값에 따라 시각적으로 변한다.

```text
totalViewTime 증가 → font-size 증가
viewCount 증가 → opacity 증가
revisitCount 증가 → letter-spacing 또는 흔들림 증가
recent activity 증가 → 잠깐 밝아짐
low activity → 서서히 흐려짐
```

### 6.3 추천 화면 문구

모니터에는 설명적 제목을 크게 붙이지 않는다.  
다만 아주 작게 다음 정도의 문구를 넣을 수 있다.

```text
<see-saw>
```

또는

```text
A text formed by reading.
```

하지만 “당신의 데이터가 수집되고 있습니다” 같은 직접 문구는 피한다.

---

## 7. 기술 스택

### 7.1 전체 구조

```text
Mobile Client: Next.js / React
Realtime Server: Node.js + Socket.IO
Display Client: React + Canvas or SVG
Database: 없음 또는 JSON 파일 저장
Deployment: 로컬 네트워크 우선
```

### 7.2 권장 스택

#### Frontend

- Next.js
- React
- TypeScript
- CSS Modules 또는 Tailwind CSS

#### Realtime

- Node.js
- Socket.IO

#### Display

- React
- SVG 또는 HTML/CSS 기반 텍스트 렌더링
- 추후 필요 시 p5.js 사용 가능

#### Storage

초기 실험에서는 DB를 쓰지 않는다.

서버 메모리에 데이터를 저장하고, 필요하면 JSON 파일로 주기적으로 저장한다.

추후 확장 시:

- SQLite
- Supabase
- Firebase
- PostgreSQL

### 7.3 로컬 실험 구성

가장 단순한 구성은 다음과 같다.

```text
노트북에서 서버 실행
        ↓
같은 노트북 브라우저에서 Display 페이지 실행
        ↓
모니터에 Display 페이지 전체화면 출력
        ↓
모바일 기기들은 같은 Wi-Fi에서 노트북 IP로 접속
```

예시 URL:

```text
Mobile:
http://192.168.0.12:3000/guide/object-01

Display:
http://192.168.0.12:3000/display
```

QR 코드는 모바일 URL을 기준으로 생성한다.

---

## 8. 폴더 구조

권장 폴더 구조:

```text
altered-seeing-prototype/
├── README.md
├── package.json
├── server/
│   ├── index.ts
│   ├── aggregator.ts
│   └── storage.ts
├── app/
│   ├── guide/
│   │   └── [pageId]/
│   │       └── page.tsx
│   ├── display/
│   │   └── page.tsx
│   └── layout.tsx
├── components/
│   ├── CuratorialText.tsx
│   ├── ReadingTracker.tsx
│   └── TextLandscape.tsx
├── data/
│   ├── texts.json
│   └── sessions.json
└── public/
    └── qr/
```

---

## 9. 주요 페이지

### 9.1 `/guide/[pageId]`

모바일 관객용 페이지.

기능:

- 큐레이션 텍스트 표시
- 각 문단 visibility 추적
- scroll depth 추적
- sessionId 생성
- Socket.IO 서버로 이벤트 전송

### 9.2 `/display`

모니터 출력용 페이지.

기능:

- 서버에서 집계 데이터 수신
- 실시간 텍스트 풍경 렌더링
- 전체화면 사용
- 마우스/키보드 조작 없이 자동 갱신

### 9.3 `/admin` 선택 사항

초기 프로토타입에서는 없어도 된다.

있다면 기능:

- 현재 연결된 세션 수 확인
- 데이터 초기화
- JSON 다운로드

단, 전시 중 관객에게 보이지 않게 한다.

---

## 10. 구현 우선순위

### Phase 1. 최소 작동 프로토타입

목표: QR을 찍으면 모바일 웹이 열리고, 읽기 시간이 모니터에 반영된다.

필수 구현:

- 모바일 큐레이션 페이지
- sessionId 생성
- 문단별 enter / exit 이벤트
- Socket.IO 연결
- 서버 집계
- display 페이지에서 문단 크기와 opacity 변화

### Phase 2. 전시 공간 테스트

목표: 실제 학과동 1층에 설치해서 사람들이 자연스럽게 QR을 찍는지 확인한다.

추가 구현:

- QR 코드 여러 개 생성
- pageId별 텍스트 분리
- 전시물별 다른 텍스트 적용
- display에서 여러 pageId 통합 시각화

### Phase 3. 작품성 강화

목표: 시각화가 단순 통계가 아니라 작품처럼 보이게 한다.

추가 구현:

- 텍스트 위치의 유기적 변화
- 최근 읽힌 문장 강조
- 오래 남은 문장의 잔상
- 읽히지 않은 문장의 소멸
- 전체 화면의 느린 호흡감

---

## 11. 개발 시 주의할 점

### 11.1 관객 경험 우선

이 프로젝트는 기술 데모가 아니다.  
기능이 많아지는 것보다, 관객이 자연스럽게 속고 자연스럽게 읽는 것이 중요하다.

### 11.2 데이터는 적게, 구조는 선명하게

처음부터 너무 많은 데이터를 수집하지 않는다.

초기에는 다음 세 가지만으로 충분하다.

- 어떤 문단을 봤는가
- 얼마나 오래 봤는가
- 어디서 멈췄는가

### 11.3 모바일 페이지의 완성도

모바일 페이지가 허술하면 작품이 작동하지 않는다.  
진짜 공식 전시 안내 페이지처럼 보여야 한다.

### 11.4 모니터 화면의 태도

모니터는 설명하지 않고 보여줘야 한다.  
관객이 “이게 뭐지?” 하고 보다가, 자신이 읽었던 문장을 발견할 수 있어야 한다.

---

## 12. 윤리와 안전

이 작업은 관객의 행동 데이터를 다루므로, 최소한의 윤리적 기준을 지켜야 한다.

권장 원칙:

- 개인 식별 정보는 수집하지 않는다.
- 위치, 카메라, 마이크 권한을 요청하지 않는다.
- 데이터는 실험 후 삭제할 수 있게 한다.
- 전시 설명 혹은 사후 설명에서 작품의 구조를 밝힐 수 있도록 준비한다.
- 학과 공간에서 진행할 경우, 최소한 지도교수 혹은 담당자의 허가를 받는다.

이 작품의 핵심은 몰래 감시하는 것이 아니라, 이미 일상화된 읽기-데이터화 구조를 전시장 안에서 감각하게 만드는 것이다.

---

## 13. 개발자에게 전달할 한 줄 브리프

> QR로 접속하는 가짜 전시 큐레이션 모바일 웹을 만들고, 관객의 문단별 읽기 시간과 스크롤 행동을 WebSocket으로 수집해, 전시장 끝 모니터에서 집단적 읽기 흔적이 실시간 텍스트 풍경으로 변형되는 프로토타입을 구현한다.

---

## 14. 성공 기준

이 프로토타입은 다음 조건을 만족하면 성공이다.

- 관객이 QR을 자연스럽게 찍는다.
- 모바일 웹이 공식 전시 안내처럼 보인다.
- 관객이 실제로 텍스트를 읽는다.
- 문단별 읽기 데이터가 실시간으로 서버에 들어온다.
- 모니터 화면이 관객 행동에 따라 변화한다.
- 설명 없이도 “이 화면이 전시장 안의 읽기 행위와 연결되어 있다”는 느낌이 생긴다.
- 작업자가 이후 어떤 데이터와 표현이 더 강한지 판단할 수 있다.

---

## 15. 당장 만들 최소 버전

가장 먼저 만들 버전은 아래 기능만 포함한다.

```text
1. /guide/object-01 페이지
2. 큐레이션 텍스트 6문단
3. 각 문단의 화면 노출 시간 측정
4. Socket.IO로 서버 전송
5. /display 페이지
6. 오래 읽힌 문장일수록 커지고 진해짐
7. QR 코드 1개 출력
8. 학과동 1층에서 1시간 테스트
```

이 최소 버전이 작동하면, 이후 QR 코드 수와 텍스트 종류를 늘린다.
